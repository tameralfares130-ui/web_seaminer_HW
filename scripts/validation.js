document.addEventListener('DOMContentLoaded',()=>{const contact=document.getElementById('contactForm');if(contact){contact.addEventListener('submit',e=>{let errors=[];const name=contact.sender_name.value.trim();const email=contact.email.value.trim();const subject=contact.subject.value.trim();const msg=contact.message.value.trim();if(!name)errors.push('Name is required');if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))errors.push('Valid email is required');if(subject.length<3)errors.push('Subject must be at least 3 characters');if(msg.length<10)errors.push('Message must be at least 10 characters');if(errors.length){e.preventDefault();alert(errors.join('\n'));}});}const reg=document.getElementById('registerForm');if(reg){reg.addEventListener('submit',e=>{if(reg.password.value.length<6){e.preventDefault();alert('Password must be at least 6 characters.');}});}});
